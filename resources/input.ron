(
    axes: {},
    actions: {
        "move_left":    [ [Key(Left)], [Key(A)] ],
        "move_right":   [ [Key(Right)], [Key(D)] ],
        "move_up":      [ [Key(Up)], [Key(W)] ],
        "move_Down":    [ [Key(Down)], [Key(S)] ],
    },
)
